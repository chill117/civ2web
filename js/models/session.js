/*global Backbone */
var app = app || {};

(function () {

	'use strict';

	app.SessionDataModel = Backbone.Model.extend({

		/*
			Example:

			{
				'id': (STRING) 'name_of_session_variable',
				'value': (MIXED) pretty much anything that can be handled by JavaScript
			}
		*/

	});

})();